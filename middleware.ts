// export { auth as middleware } from "next-auth/middleware";

// NOTE: 開発中はオフ
// export const config = {
//   matcher: ['/dashboard/:path*', '/api/prompts/:path*'],
// };

export const config = {
  matcher: [],
};
